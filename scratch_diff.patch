diff --git a/edge/dashboard/static/css/style.css b/edge/dashboard/static/css/style.css
index 1719b4c..0baa4c3 100644
--- a/edge/dashboard/static/css/style.css
+++ b/edge/dashboard/static/css/style.css
@@ -1,3 +1,13 @@
+@font-face { font-family: 'Inter'; font-style: normal; font-weight: 300; font-display: swap; src: url('/static/fonts/inter-300.woff2') format('woff2'); }
+@font-face { font-family: 'Inter'; font-style: normal; font-weight: 400; font-display: swap; src: url('/static/fonts/inter-400.woff2') format('woff2'); }
+@font-face { font-family: 'Inter'; font-style: normal; font-weight: 500; font-display: swap; src: url('/static/fonts/inter-500.woff2') format('woff2'); }
+@font-face { font-family: 'Inter'; font-style: normal; font-weight: 600; font-display: swap; src: url('/static/fonts/inter-600.woff2') format('woff2'); }
+@font-face { font-family: 'Inter'; font-style: normal; font-weight: 700; font-display: swap; src: url('/static/fonts/inter-700.woff2') format('woff2'); }
+
+@font-face { font-family: 'Noto Sans Arabic'; font-style: normal; font-weight: 400; font-display: swap; src: url('/static/fonts/noto-sans-arabic-400.woff2') format('woff2'); }
+@font-face { font-family: 'Noto Sans Arabic'; font-style: normal; font-weight: 500; font-display: swap; src: url('/static/fonts/noto-sans-arabic-500.woff2') format('woff2'); }
+@font-face { font-family: 'Noto Sans Arabic'; font-style: normal; font-weight: 700; font-display: swap; src: url('/static/fonts/noto-sans-arabic-700.woff2') format('woff2'); }
+
 :root {
   /* Dark Color Palette (Default) */
   --bg-dark: #0F172A;
@@ -38,7 +48,7 @@ body.theme-light {
 }
 
 body[dir="rtl"] {
-  font-family: 'Inter', 'Cairo', sans-serif; /* Adding Cairo for Arabic if available */
+  font-family: 'Noto Sans Arabic', 'Inter', sans-serif;
 }
 
 body[dir="rtl"] .sidebar {
diff --git a/edge/dashboard/templates/base.html b/edge/dashboard/templates/base.html
index 95de26e..db32df4 100644
--- a/edge/dashboard/templates/base.html
+++ b/edge/dashboard/templates/base.html
@@ -5,13 +5,10 @@
     <meta name="viewport" content="width=device-width, initial-scale=1.0">
     <title>{% block title %}Dashboard{% endblock %} | Edge Admin</title>
     
-    <!-- Google Fonts: Inter -->
-    <link rel="preconnect" href="https://fonts.googleapis.com">
-    <link rel="preconnect" href="https://fonts.gstatic.com" crossorigin>
-    <link href="https://fonts.googleapis.com/css2?family=Inter:wght@300;400;500;600;700&display=swap" rel="stylesheet">
+    <!-- Google Fonts: Inter (Removed for Offline Migration) -->
     
     <!-- Phosphor Icons -->
-    <script src="https://unpkg.com/@phosphor-icons/web"></script>
+    <script src="/static/vendor/phosphor/index.js"></script>
     
     <!-- Custom CSS -->
     <link rel="stylesheet" href="/static/css/style.css?v=4">
diff --git a/edge/dashboard/templates/status.html b/edge/dashboard/templates/status.html
index c3039fe..71d872c 100644
--- a/edge/dashboard/templates/status.html
+++ b/edge/dashboard/templates/status.html
@@ -1,63 +1,6 @@
 {% extends "base.html" %}
-{% block title %}System Status
-<script>
-/* STATUS_RUNTIME_METRICS_JS_START */
-(function () {
-    function setText(id, value) {
-        const el = document.getElementById(id);
-        if (el) el.textContent = value;
-    }
-
-    function setBar(id, value) {
-        const el = document.getElementById(id);
-        if (el) el.style.width = Math.max(0, Math.min(100, value)) + "%";
-    }
+{% block title %}System Status{% endblock %}
 
-    function fmt(value) {
-        const n = Number(value);
-        if (!Number.isFinite(n)) return "--";
-        return n.toFixed(1).replace(".0", "");
-    }
-
-    async function loadRuntimeMetrics() {
-        try {
-            const res = await fetch("/api/system/runtime?ts=" + Date.now(), { cache: "no-store" });
-            if (!res.ok) return;
-
-            const data = await res.json();
-
-            if (data.cpu_percent !== null && data.cpu_percent !== undefined) {
-                setText("runtimeCpuText", fmt(data.cpu_percent) + "%");
-                setBar("runtimeCpuBar", Number(data.cpu_percent));
-            }
-
-            if (data.memory_percent !== null && data.memory_percent !== undefined) {
-                setText(
-                    "runtimeMemoryText",
-                    fmt(data.memory_used_gb) + " / " + fmt(data.memory_total_gb) + " GB (" + fmt(data.memory_percent) + "%)"
-                );
-                setBar("runtimeMemoryBar", Number(data.memory_percent));
-            }
-
-            if (data.storage_percent !== null && data.storage_percent !== undefined) {
-                setText(
-                    "runtimeStorageText",
-                    fmt(data.storage_used_gb) + " / " + fmt(data.storage_total_gb) + " GB (" + fmt(data.storage_percent) + "%)"
-                );
-                setBar("runtimeStorageBar", Number(data.storage_percent));
-            }
-        } catch (e) {
-            console.warn("Runtime metrics failed:", e);
-        }
-    }
-
-    document.addEventListener("DOMContentLoaded", loadRuntimeMetrics);
-    setInterval(loadRuntimeMetrics, 15000);
-})();
-/* STATUS_RUNTIME_METRICS_JS_END */
-</script>
-
-{% endblock %}
 
 {% block content %}
 <div class="page-header">
@@ -175,21 +118,21 @@
                 <i class="ph-fill ph-broadcast" style="color: var(--accent-primary);"></i>
                 <span data-i18n="mqtt_broker">MQTT Broker</span>
             </h3>
-            <span class="badge badge-success" data-i18n="badge_healthy">Healthy</span>
+            <span class="badge badge-success" id="mqttHealthBadge" data-i18n="badge_healthy">Healthy</span>
         </div>
 
         <div class="status-lines-clean">
             <div class="status-line-clean">
                 <span class="status-label-clean">MQTT Broker:</span>
-                <span class="status-value-clean">smarthome.local:1883</span>
+                <span class="status-value-clean" id="mqttBrokerHost">Loading...</span>
             </div>
             <div class="status-line-clean">
                 <span class="status-label-clean" data-i18n="label_status">Status:</span>
-                <span class="status-value-clean" data-i18n="status_connected">Connected</span>
+                <span class="status-value-clean" id="mqttStatus" data-i18n="status_connected">Loading...</span>
             </div>
             <div class="status-line-clean">
                 <span class="status-label-clean" data-i18n="label_messages_min">Messages/min:</span>
-                <span class="status-value-clean">~145</span>
+                <span class="status-value-clean" id="mqttMsgs">-</span>
             </div>
         </div>
     </div>
@@ -201,21 +144,21 @@
                 <i class="ph-fill ph-hard-drives" style="color: var(--accent-primary);"></i>
                 <span data-i18n="edge_server">Edge Server</span>
             </h3>
-            <span class="badge badge-success" data-i18n="badge_running">Running</span>
+            <span class="badge badge-success" id="serverHealthBadge" data-i18n="badge_running">Running</span>
         </div>
 
         <div class="status-lines-clean">
             <div class="status-line-clean">
                 <span class="status-label-clean">Server URL:</span>
-                <span class="status-value-clean">http://smarthome.local:8000</span>
+                <span class="status-value-clean" id="serverUrl">Loading...</span>
             </div>
             <div class="status-line-clean">
                 <span class="status-label-clean">Current LAN IP:</span>
-                <span class="status-value-clean">192.168.1.100</span>
+                <span class="status-value-clean" id="lanIp">Loading...</span>
             </div>
             <div class="status-line-clean">
                 <span class="status-label-clean">API Status:</span>
-                <span class="status-value-clean" style="color: var(--status-success);" data-i18n="status_online">Online</span>
+                <span class="status-value-clean" id="apiStatus" style="color: var(--status-success);" data-i18n="status_online">Loading...</span>
             </div>
 
             <hr class="status-divider-clean">
@@ -253,21 +196,21 @@
                 <i class="ph-fill ph-database" style="color: var(--accent-primary);"></i>
                 <span data-i18n="sqlite_db">SQLite Database</span>
             </h3>
-            <span class="badge badge-success" data-i18n="badge_healthy">Healthy</span>
+            <span class="badge badge-success" id="sqliteHealthBadge" data-i18n="badge_healthy">Healthy</span>
         </div>
 
         <div class="status-lines-clean">
             <div class="status-line-clean">
                 <span class="status-label-clean" data-i18n="label_status">Status:</span>
-                <span class="status-value-clean" data-i18n="status_connected">Connected</span>
+                <span class="status-value-clean" id="sqliteStatus" data-i18n="status_connected">Loading...</span>
             </div>
             <div class="status-line-clean">
                 <span class="status-label-clean" data-i18n="label_size">Size:</span>
-                <span class="status-value-clean">14.2 MB</span>
+                <span class="status-value-clean" id="sqliteSize">-</span>
             </div>
             <div class="status-line-clean">
                 <span class="status-label-clean" data-i18n="label_location">Location:</span>
-                <span class="status-value-clean" style="font-size: 0.8rem;">/database/smart_home_edge.db</span>
+                <span class="status-value-clean" id="sqliteLocation" style="font-size: 0.8rem;">Loading...</span>
             </div>
         </div>
     </div>
@@ -279,18 +222,16 @@
         <span>Recent Infrastructure Logs</span>
     </h3>
 
-    <div class="infra-log-box-clean">
-        <div style="margin-bottom: 4px;">[INFO] 2026-05-13 20:30:15 - MQTT broker connection checked.</div>
-        <div style="margin-bottom: 4px;">[INFO] 2026-05-13 20:30:16 - Edge server API is online.</div>
-        <div style="margin-bottom: 4px;">[INFO] 2026-05-13 20:30:17 - SQLite database connection verified.</div>
-        <div style="margin-bottom: 4px;">[INFO] 2026-05-13 20:35:22 - Infrastructure status refresh completed.</div>
-        <div style="margin-bottom: 4px; color: #FCD34D;">[WARN] 2026-05-13 20:42:10 - Hardware runtime metrics will be fully real after deployment.</div>
+    <div class="infra-log-box-clean" id="infraLogs">
+        <div style="margin-bottom: 4px; color: #94A3B8;">Loading real infrastructure logs...</div>
     </div>
 </div>
 
 <script>
 /* STATUS_RUNTIME_METRICS_JS_START */
 (function () {
+    let _fetching = false;
+
     function setText(id, value) {
         const el = document.getElementById(id);
         if (el) el.textContent = value;
@@ -301,6 +242,13 @@
         if (el) el.style.width = Math.max(0, Math.min(100, value)) + "%";
     }
 
+    function setBadge(id, isHealthy, text) {
+        const el = document.getElementById(id);
+        if (!el) return;
+        el.className = isHealthy ? "badge badge-success" : "badge badge-danger";
+        el.textContent = text;
+    }
+
     function fmt(value) {
         const n = Number(value);
         if (!Number.isFinite(n)) return "--";
@@ -308,39 +256,81 @@
     }
 
     async function loadRuntimeMetrics() {
+        if (_fetching) return;
+        _fetching = true;
         try {
-            const res = await fetch("/api/system/runtime?ts=" + Date.now(), { cache: "no-store" });
-            if (!res.ok) return;
+            const res = await fetch("/api/system-status/live?ts=" + Date.now(), { cache: "no-store" });
+            if (!res.ok) throw new Error("API not ok");
 
             const data = await res.json();
 
-            if (data.cpu_percent !== null && data.cpu_percent !== undefined) {
-                setText("runtimeCpuText", fmt(data.cpu_percent) + "%");
-                setBar("runtimeCpuBar", Number(data.cpu_percent));
+            // MQTT
+            if (data.mqtt) {
+                setText("mqttBrokerHost", data.mqtt.host + ":" + data.mqtt.port);
+                setText("mqttStatus", data.mqtt.status);
+                setText("mqttMsgs", data.mqtt.messages_per_min);
+                setBadge("mqttHealthBadge", data.mqtt.health === "healthy", data.mqtt.health === "healthy" ? "Healthy" : "Error");
+            }
+
+            // Server
+            if (data.server) {
+                setText("serverUrl", data.server.server_url);
+                setText("lanIp", data.server.lan_ip);
+                setText("apiStatus", data.server.api_status);
+                setBadge("serverHealthBadge", data.server.status === "Running", data.server.status);
+
+                if (data.server.cpu_percent !== null && data.server.cpu_percent !== undefined) {
+                    setText("runtimeCpuText", fmt(data.server.cpu_percent) + "%");
+                    setBar("runtimeCpuBar", Number(data.server.cpu_percent));
+                }
+
+                if (data.server.memory_percent !== null && data.server.memory_percent !== undefined) {
+                    setText("runtimeMemoryText", fmt(data.server.memory_used) + " / " + fmt(data.server.memory_total) + " GB (" + fmt(data.server.memory_percent) + "%)");
+                    setBar("runtimeMemoryBar", Number(data.server.memory_percent));
+                }
+
+                if (data.server.storage_percent !== null && data.server.storage_percent !== undefined) {
+                    setText("runtimeStorageText", fmt(data.server.storage_used) + " / " + fmt(data.server.storage_total) + " GB (" + fmt(data.server.storage_percent) + "%)");
+                    setBar("runtimeStorageBar", Number(data.server.storage_percent));
+                }
             }
 
-            if (data.memory_percent !== null && data.memory_percent !== undefined) {
-                setText(
-                    "runtimeMemoryText",
-                    fmt(data.memory_used_gb) + " / " + fmt(data.memory_total_gb) + " GB (" + fmt(data.memory_percent) + "%)"
-                );
-                setBar("runtimeMemoryBar", Number(data.memory_percent));
+            // SQLite
+            if (data.sqlite) {
+                setText("sqliteStatus", data.sqlite.status);
+                setText("sqliteSize", data.sqlite.size);
+                setText("sqliteLocation", data.sqlite.location);
+                setBadge("sqliteHealthBadge", data.sqlite.health === "healthy", data.sqlite.health === "healthy" ? "Healthy" : "Warning");
             }
 
-            if (data.storage_percent !== null && data.storage_percent !== undefined) {
-                setText(
-                    "runtimeStorageText",
-                    fmt(data.storage_used_gb) + " / " + fmt(data.storage_total_gb) + " GB (" + fmt(data.storage_percent) + "%)"
-                );
-                setBar("runtimeStorageBar", Number(data.storage_percent));
+            // Logs
+            if (data.system_logs && Array.isArray(data.system_logs)) {
+                const logsBox = document.getElementById("infraLogs");
+                if (logsBox) {
+                    logsBox.innerHTML = data.system_logs.map(log => `<div style="margin-bottom: 4px;">${log}</div>`).join("");
+                }
             }
+
         } catch (e) {
             console.warn("Runtime metrics failed:", e);
+        } finally {
+            _fetching = false;
         }
     }
 
     document.addEventListener("DOMContentLoaded", loadRuntimeMetrics);
-    setInterval(loadRuntimeMetrics, 15000);
+    // Overwrite the manual refresh button
+    const btnRefresh = document.querySelector('button[onclick="window.location.reload()"]');
+    if (btnRefresh) {
+        btnRefresh.onclick = (e) => {
+            e.preventDefault();
+            btnRefresh.classList.add("loading");
+            loadRuntimeMetrics().then(() => {
+                btnRefresh.classList.remove("loading");
+            });
+        };
+    }
+    setInterval(loadRuntimeMetrics, 3000);
 })();
 /* STATUS_RUNTIME_METRICS_JS_END */
 </script>
diff --git a/edge/main.py b/edge/main.py
index b6f1158..528d4f4 100644
--- a/edge/main.py
+++ b/edge/main.py
@@ -3078,19 +3078,16 @@ def api_system_status_live():
         except Exception:
             port = 1883
 
-        connected = False
-        try:
-            with socket.create_connection((host, port), timeout=0.35):
-                connected = True
-        except Exception:
-            connected = False
+        from mqtt import mqtt_client
+        connected = mqtt_client.is_connected()
+        msgs_min = mqtt_client.get_messages_per_min()
 
         return {
             "host": host,
             "port": port,
             "status": "Connected" if connected else "Not connected",
             "health": "healthy" if connected else "warning",
-            "messages_per_min": "~0" if not connected else "~ live",
+            "messages_per_min": str(msgs_min),
         }
 
     metrics = _memory_cpu()
@@ -3111,7 +3108,7 @@ def api_system_status_live():
         "server": {
             "status": "Running",
             "api_status": "Online",
-            "server_url": "http://127.0.0.1:8000",
+            "server_url": f"http://{_lan_ip()}:8000",
             "lan_ip": _lan_ip(),
             **metrics,
         },
diff --git a/edge/mqtt/mqtt_client.py b/edge/mqtt/mqtt_client.py
index d225a02..1744db1 100644
--- a/edge/mqtt/mqtt_client.py
+++ b/edge/mqtt/mqtt_client.py
@@ -1,5 +1,7 @@
 import time
 import logging
+from collections import deque
+import threading
 
 # pyrefly: ignore [missing-import]
 import paho.mqtt.client as mqtt
@@ -43,6 +45,9 @@ class MQTTClient:
         self.callbacks: Dict[str, Callable] = {}
 
         self._connected = False
+        
+        self._msg_timestamps = deque()
+        self._msg_lock = threading.Lock()
 
     # =========================
     # CONNECTION EVENTS
@@ -114,6 +119,9 @@ class MQTTClient:
             "utf-8"
         )
 
+        with self._msg_lock:
+            self._msg_timestamps.append(time.time())
+
         logger.debug(
             f"[MQTT] {topic} ΓåÆ {payload}"
         )
@@ -298,4 +306,11 @@ class MQTTClient:
 
     def is_connected(self) -> bool:
 
-        return self._connected
\ No newline at end of file
+        return self._connected
+
+    def get_messages_per_min(self) -> int:
+        with self._msg_lock:
+            now = time.time()
+            while self._msg_timestamps and self._msg_timestamps[0] < now - 60:
+                self._msg_timestamps.popleft()
+            return len(self._msg_timestamps)
\ No newline at end of file
diff --git a/edge/services/image_upload_service.py b/edge/services/image_upload_service.py
index ccaed06..20fcd16 100644
--- a/edge/services/image_upload_service.py
+++ b/edge/services/image_upload_service.py
@@ -46,10 +46,38 @@ def publish_device_event(
     payload: dict
 ):
     """
-    FUTURE INTEGRATION HOOK:
-    MQTT & Flutter Notifications
+    MQTT & Flutter Notifications Integration
     """
-    pass
+    try:
+        from mqtt import mqtt_client
+        home_id = payload.get("home_id")
+        face_result = payload.get("face_result", {})
+        
+        # Publish critical alert for Unknown Person
+        if face_result and face_result.get("success") and not face_result.get("matched"):
+            # If home_id is integer, format it. If string, leave it.
+            try:
+                formatted_home_id = f"HOME-{int(home_id):03d}"
+            except (ValueError, TypeError):
+                formatted_home_id = str(home_id)
+                
+            topic = f"home/{formatted_home_id}/alerts/critical"
+            
+            import json
+            from datetime import datetime
+            
+            alert_payload = {
+                "type": "UNKNOWN_PERSON",
+                "device_id": device_id,
+                "timestamp": datetime.now().isoformat(),
+                "image_path": payload.get("image_path")
+            }
+            
+            mqtt_client.publish(topic, json.dumps(alert_payload))
+            print(f"[MQTT ALERT] Published UNKNOWN_PERSON to {topic}")
+            
+    except Exception as e:
+        print(f"[MQTT ALERT ERROR] Failed to publish: {e}")
 
 
 class ImageUploadService:
diff --git a/firmware/energy_meter/secrets.h b/firmware/energy_meter/secrets.h
index a69d88b..751e843 100644
--- a/firmware/energy_meter/secrets.h
+++ b/firmware/energy_meter/secrets.h
@@ -1,8 +1,8 @@
 #pragma once
 
-const char* WIFI_SSID = "dlink-38D4";
-const char* WIFI_PASSWORD = "T5NetnrN";
+const char* WIFI_SSID = "BOLBA(235)";
+const char* WIFI_PASSWORD = "";
 
-const char* SERVER_IP = "10.0.0.22";
+const char* SERVER_IP = "172.17.235.4";
 
-const char* DEVICE_TOKEN = "tok_8e4c996e6ab83cae";
\ No newline at end of file
+const char* DEVICE_TOKEN = "tok_e10ffdc24215e9d3";
\ No newline at end of file
diff --git a/firmware/esp32_cam/esp32_cam.ino b/firmware/esp32_cam/esp32_cam.ino
index afbe22e..6c73d5e 100644
--- a/firmware/esp32_cam/esp32_cam.ino
+++ b/firmware/esp32_cam/esp32_cam.ino
@@ -15,7 +15,7 @@
 // Removed device_id in favor of DEVICE_TOKEN from secrets.h
 
 // ================= MQTT =================
-const char* mqtt_server = "10.0.0.23";
+const char* mqtt_server = "172.17.235.4";
 const int mqtt_port = 1883;
 
 WiFiClient espClient;
@@ -38,12 +38,22 @@ Servo doorServo;
 #define ECHO_PIN 14
 
 const int DETECTION_DISTANCE = 100;
-const unsigned long COOLDOWN_MS = 10000;
+const unsigned long PRESENCE_DEBOUNCE_MS = 1000;
+const unsigned long LEAVE_DEBOUNCE_MS = 5000;
 const unsigned long HEARTBEAT_INTERVAL_MS = 30000;
 
-unsigned long lastUploadTime = 0;
 unsigned long lastHeartbeatTime = 0;
 
+enum SystemState {
+  STATE_IDLE,
+  STATE_DETECTING_PRESENCE,
+  STATE_WAITING_FOR_LEAVE,
+  STATE_CONFIRMING_LEAVE
+};
+
+SystemState currentState = STATE_IDLE;
+unsigned long stateChangeTime = 0;
+
 // ================= CAMERA PINS (AI THINKER) =================
 
 #define PWDN_GPIO_NUM     32
@@ -521,86 +531,86 @@ Serial.println("SERVO READY");
 
 void loop() {
 
-  if (
-      WiFi.status() != WL_CONNECTED
-  ) {
-
-    Serial.println(
-        "WiFi Lost"
-    );
-
+  if (WiFi.status() != WL_CONNECTED) {
+    Serial.println("WiFi Lost");
     connectWiFi();
   }
   if (!mqttClient.connected()) {
+    connectMQTT();
+  }
 
-  connectMQTT();
-}
-
-mqttClient.loop();
-
-  float distance =
-      getDistanceCM();
-
-  Serial.print(
-      "Distance: "
-  );
-
-  Serial.println(
-      distance
-  );
-
-  bool objectDetected =
-      distance > 0 &&
-      distance < DETECTION_DISTANCE;
-
-  bool cooldownFinished =
-      millis() -
-          lastUploadTime >
-      COOLDOWN_MS;
-
-  if (
-      objectDetected &&
-      cooldownFinished
-  ) {
-
-    Serial.println(
-        "OBJECT DETECTED"
-    );
-
-    delay(300);
-
-   captureAndUpload();
-
-Serial.print("MQTT STATE AFTER POST = ");
-Serial.println(mqttClient.state());
-
-Serial.println("WAITING FOR MQTT RESPONSE");
-
-    unsigned long startWait = millis();
-
-    while (millis() - startWait < 5000) {
-
-    bool ok = mqttClient.loop();
-
-    if (ok) {
-        Serial.println("MQTT LOOP OK");
-    }
-
-    delay(10);
-}
-
-    Serial.println("MQTT WAIT FINISHED");
-
-    lastUploadTime =
-        millis();
+  mqttClient.loop();
+
+  float distance = getDistanceCM();
+  
+  // Debug output can be noisy, so we print occasionally or only on state change,
+  // but to keep previous behavior we can print it here.
+  // Serial.print("Distance: ");
+  // Serial.println(distance);
+
+  bool objectPresent = (distance > 0 && distance < DETECTION_DISTANCE);
+  unsigned long currentMillis = millis();
+
+  switch (currentState) {
+    case STATE_IDLE:
+      if (objectPresent) {
+        currentState = STATE_DETECTING_PRESENCE;
+        stateChangeTime = currentMillis;
+        Serial.println("Object detected, confirming presence...");
+      }
+      break;
+
+    case STATE_DETECTING_PRESENCE:
+      if (!objectPresent) {
+        // False alarm (e.g. noisy reading)
+        currentState = STATE_IDLE;
+        Serial.println("Presence lost, returning to IDLE.");
+      } else if (currentMillis - stateChangeTime >= PRESENCE_DEBOUNCE_MS) {
+        // Confirmed presence!
+        Serial.println("Presence CONFIRMED!");
+        captureAndUpload();
+
+        Serial.println("WAITING FOR MQTT RESPONSE");
+        unsigned long startWait = millis();
+        while (millis() - startWait < 5000) {
+          mqttClient.loop();
+          delay(10);
+        }
+        Serial.println("MQTT WAIT FINISHED");
+
+        currentState = STATE_WAITING_FOR_LEAVE;
+        Serial.println("Now waiting for person to leave...");
+      }
+      break;
+
+    case STATE_WAITING_FOR_LEAVE:
+      if (!objectPresent) {
+        currentState = STATE_CONFIRMING_LEAVE;
+        stateChangeTime = currentMillis;
+        Serial.println("Person left, confirming leave...");
+      }
+      break;
+
+    case STATE_CONFIRMING_LEAVE:
+      if (objectPresent) {
+        // Person came back or noisy reading
+        currentState = STATE_WAITING_FOR_LEAVE;
+        Serial.println("Person still here, cancelling leave confirmation.");
+      } else if (currentMillis - stateChangeTime >= LEAVE_DEBOUNCE_MS) {
+        // Confirmed left
+        currentState = STATE_IDLE;
+        Serial.println("Leave CONFIRMED. System rearmed to IDLE.");
+      }
+      break;
   }
+
   // Publish heartbeat to keep backend's last_seen current
-  if (millis() - lastHeartbeatTime > HEARTBEAT_INTERVAL_MS) {
+  if (currentMillis - lastHeartbeatTime > HEARTBEAT_INTERVAL_MS) {
     String statusTopic = "device/" + String(DEVICE_TOKEN) + "/status";
     String heartbeat = "{\"device_token\":\"" + String(DEVICE_TOKEN) + "\",\"state\":\"online\"}";
     mqttClient.publish(statusTopic.c_str(), heartbeat.c_str());
     Serial.println("HEARTBEAT SENT");
-    lastHeartbeatTime = millis();
+    lastHeartbeatTime = currentMillis;
   }
 
   delay(300);
diff --git a/firmware/esp32_cam/secrets.h b/firmware/esp32_cam/secrets.h
index 98b2c04..d84a6af 100644
--- a/firmware/esp32_cam/secrets.h
+++ b/firmware/esp32_cam/secrets.h
@@ -1,8 +1,8 @@
 #pragma once
 
-#define WIFI_SSID "BolbaTop"
-#define WIFI_PASSWORD "abdellah"
+#define WIFI_SSID "BOLBA(235)"
+#define WIFI_PASSWORD ""
 
-#define SERVER_URL "http://10.0.0.23:8000/api/device/upload-image"
+#define SERVER_URL "http://172.17.235.4:8000/api/device/upload-image"
 
 #define DEVICE_TOKEN "tok_4bba7920b5077004"
\ No newline at end of file
diff --git a/smart_home_app/android/app/build.gradle.kts b/smart_home_app/android/app/build.gradle.kts
index a48d205..cf3c840 100644
--- a/smart_home_app/android/app/build.gradle.kts
+++ b/smart_home_app/android/app/build.gradle.kts
@@ -11,6 +11,7 @@ android {
     ndkVersion = flutter.ndkVersion
 
     compileOptions {
+        isCoreLibraryDesugaringEnabled = true
         sourceCompatibility = JavaVersion.VERSION_11
         targetCompatibility = JavaVersion.VERSION_11
     }
@@ -42,3 +43,7 @@ android {
 flutter {
     source = "../.."
 }
+
+dependencies {
+    coreLibraryDesugaring("com.android.tools:desugar_jdk_libs:2.1.5")
+}
diff --git a/smart_home_app/android/app/src/main/AndroidManifest.xml b/smart_home_app/android/app/src/main/AndroidManifest.xml
index 7bdb33f..14a5999 100644
--- a/smart_home_app/android/app/src/main/AndroidManifest.xml
+++ b/smart_home_app/android/app/src/main/AndroidManifest.xml
@@ -1,5 +1,12 @@
 <manifest 
     xmlns:android="http://schemas.android.com/apk/res/android">
+    <uses-permission android:name="android.permission.INTERNET" />
+    <uses-permission android:name="android.permission.POST_NOTIFICATIONS" />
+    <uses-permission android:name="android.permission.WAKE_LOCK" />
+    <uses-permission android:name="android.permission.FOREGROUND_SERVICE" />
+    <uses-permission android:name="android.permission.FOREGROUND_SERVICE_DATA_SYNC" />
+    <uses-permission android:name="android.permission.RECEIVE_BOOT_COMPLETED" />
+    
     <uses-permission android:name="android.permission.USE_BIOMETRIC" />
     <uses-permission android:name="android.permission.CAMERA" />
     <uses-permission android:name="android.permission.READ_EXTERNAL_STORAGE" />
@@ -36,6 +43,13 @@
         <meta-data
             android:name="flutterEmbedding"
             android:value="2" />
+            
+        <!-- Background Service Declaration -->
+        <service
+            android:name="id.flutter.flutter_background_service.BackgroundService"
+            android:foregroundServiceType="dataSync"
+            android:exported="true" />
+            
     </application>
     <!-- Required to query activities that can process text, see:
          https://developer.android.com/training/package-visibility and
diff --git a/smart_home_app/android/gradle.properties b/smart_home_app/android/gradle.properties
index 475a628..75f1cda 100644
--- a/smart_home_app/android/gradle.properties
+++ b/smart_home_app/android/gradle.properties
@@ -5,3 +5,4 @@ android.enableJetifier=true
 android.builtInKotlin=false
 # This newDsl flag was added automatically by Flutter migrator
 android.newDsl=false
+kotlin.incremental=false
\ No newline at end of file
diff --git a/smart_home_app/lib/main.dart b/smart_home_app/lib/main.dart
index a80ef5b..b781553 100644
--- a/smart_home_app/lib/main.dart
+++ b/smart_home_app/lib/main.dart
@@ -1,20 +1,34 @@
 import 'package:flutter/material.dart';
 import 'package:provider/provider.dart';
 import 'package:flutter_localizations/flutter_localizations.dart';
+import 'package:permission_handler/permission_handler.dart';
 import 'l10n/app_localizations.dart';
 
 import 'theme/app_theme.dart';
 import 'providers/app_state_provider.dart';
 import 'screens/main_layout.dart';
 import 'screens/splash_screen.dart';
+import 'services/background_notification_service.dart';
 
-void main() {
+void main() async {
+  WidgetsFlutterBinding.ensureInitialized();
+  
   runApp(
     MultiProvider(
       providers: [ChangeNotifierProvider(create: (_) => AppStateProvider())],
       child: const SmartHomeApp(),
     ),
   );
+
+  // Initialize notifications independently in the background
+  Future.microtask(() async {
+    try {
+      await Permission.notification.request();
+      await BackgroundNotificationService.initialize();
+    } catch (e) {
+      debugPrint('Failed to initialize background service: $e');
+    }
+  });
 }
 
 class SmartHomeApp extends StatelessWidget {
diff --git a/smart_home_app/lib/screens/splash_screen.dart b/smart_home_app/lib/screens/splash_screen.dart
index b069da0..688e826 100644
--- a/smart_home_app/lib/screens/splash_screen.dart
+++ b/smart_home_app/lib/screens/splash_screen.dart
@@ -1,6 +1,5 @@
 import 'package:flutter/material.dart';
 import 'package:provider/provider.dart';
-import 'package:google_fonts/google_fonts.dart';
 import 'dart:async';
 
 import '../providers/app_state_provider.dart';
@@ -132,7 +131,8 @@ class _SplashScreenState extends State<SplashScreen> with TickerProviderStateMix
                     opacity: _textFadeAnim.value,
                     child: Text(
                       'Edge',
-                      style: GoogleFonts.montserrat(
+                      style: TextStyle(
+                        fontFamily: 'Montserrat',
                         fontSize: 56,
                         fontWeight: FontWeight.w300, // Elegant lightweight font
                         letterSpacing: 16.0,
diff --git a/smart_home_app/lib/theme/app_theme.dart b/smart_home_app/lib/theme/app_theme.dart
index f2c66f7..4f270ea 100644
--- a/smart_home_app/lib/theme/app_theme.dart
+++ b/smart_home_app/lib/theme/app_theme.dart
@@ -20,7 +20,7 @@ class AppTheme {
   static const Color darkTextSecondary = Color(0xFF94A3B8);
 
   static ThemeData lightTheme(Locale locale) {
-    String? fontFamily = locale.languageCode == 'ar' ? null : 'Inter';
+    String? fontFamily = locale.languageCode == 'ar' ? 'Noto Sans Arabic' : 'Inter';
 
     return ThemeData(
       brightness: Brightness.light,
@@ -98,7 +98,7 @@ class AppTheme {
   }
 
   static ThemeData darkTheme(Locale locale) {
-    String? fontFamily = locale.languageCode == 'ar' ? null : 'Inter';
+    String? fontFamily = locale.languageCode == 'ar' ? 'Noto Sans Arabic' : 'Inter';
 
     return ThemeData(
       brightness: Brightness.dark,
diff --git a/smart_home_app/pubspec.lock b/smart_home_app/pubspec.lock
index 3114157..b593845 100644
--- a/smart_home_app/pubspec.lock
+++ b/smart_home_app/pubspec.lock
@@ -153,6 +153,22 @@ packages:
       url: "https://pub.dev"
     source: hosted
     version: "1.0.8"
+  dbus:
+    dependency: transitive
+    description:
+      name: dbus
+      sha256: "0ce9b0a839e6dee59a37a623d2fc26a35bbbe6404213e419b0d6411023d62645"
+      url: "https://pub.dev"
+    source: hosted
+    version: "0.7.14"
+  event_bus:
+    dependency: transitive
+    description:
+      name: event_bus
+      sha256: "1a55e97923769c286d295240048fc180e7b0768902c3c2e869fe059aafa15304"
+      url: "https://pub.dev"
+    source: hosted
+    version: "2.0.1"
   fake_async:
     dependency: transitive
     description:
@@ -214,6 +230,38 @@ packages:
     description: flutter
     source: sdk
     version: "0.0.0"
+  flutter_background_service:
+    dependency: "direct main"
+    description:
+      name: flutter_background_service
+      sha256: "70a1c185b1fa1a44f8f14ecd6c86f6e50366e3562f00b2fa5a54df39b3324d3d"
+      url: "https://pub.dev"
+    source: hosted
+    version: "5.1.0"
+  flutter_background_service_android:
+    dependency: transitive
+    description:
+      name: flutter_background_service_android
+      sha256: ca0793d4cd19f1e194a130918401a3d0b1076c81236f7273458ae96987944a87
+      url: "https://pub.dev"
+    source: hosted
+    version: "6.3.1"
+  flutter_background_service_ios:
+    dependency: transitive
+    description:
+      name: flutter_background_service_ios
+      sha256: "6037ffd45c4d019dab0975c7feb1d31012dd697e25edc05505a4a9b0c7dc9fba"
+      url: "https://pub.dev"
+    source: hosted
+    version: "5.0.3"
+  flutter_background_service_platform_interface:
+    dependency: transitive
+    description:
+      name: flutter_background_service_platform_interface
+      sha256: ca74aa95789a8304f4d3f57f07ba404faa86bed6e415f83e8edea6ad8b904a41
+      url: "https://pub.dev"
+    source: hosted
+    version: "5.1.2"
   flutter_iconly:
     dependency: "direct main"
     description:
@@ -238,6 +286,38 @@ packages:
       url: "https://pub.dev"
     source: hosted
     version: "5.0.0"
+  flutter_local_notifications:
+    dependency: "direct main"
+    description:
+      name: flutter_local_notifications
+      sha256: "2b50e938a275e1ad77352d6a25e25770f4130baa61eaf02de7a9a884680954ad"
+      url: "https://pub.dev"
+    source: hosted
+    version: "20.1.0"
+  flutter_local_notifications_linux:
+    dependency: transitive
+    description:
+      name: flutter_local_notifications_linux
+      sha256: dce0116868cedd2cdf768af0365fc37ff1cbef7c02c4f51d0587482e625868d0
+      url: "https://pub.dev"
+    source: hosted
+    version: "7.0.0"
+  flutter_local_notifications_platform_interface:
+    dependency: transitive
+    description:
+      name: flutter_local_notifications_platform_interface
+      sha256: "23de31678a48c084169d7ae95866df9de5c9d2a44be3e5915a2ff067aeeba899"
+      url: "https://pub.dev"
+    source: hosted
+    version: "10.0.0"
+  flutter_local_notifications_windows:
+    dependency: transitive
+    description:
+      name: flutter_local_notifications_windows
+      sha256: e97a1a3016512437d9c0b12fae7d1491c3c7b9aa7f03a69b974308840656b02a
+      url: "https://pub.dev"
+    source: hosted
+    version: "2.0.1"
   flutter_localizations:
     dependency: "direct main"
     description: flutter
@@ -317,14 +397,6 @@ packages:
     description: flutter
     source: sdk
     version: "0.0.0"
-  google_fonts:
-    dependency: "direct main"
-    description:
-      name: google_fonts
-      sha256: "517b20870220c48752eafa0ba1a797a092fb22df0d89535fd9991e86ee2cdd9c"
-      url: "https://pub.dev"
-    source: hosted
-    version: "6.3.2"
   html:
     dependency: transitive
     description:
@@ -557,6 +629,14 @@ packages:
       url: "https://pub.dev"
     source: hosted
     version: "2.0.0"
+  mqtt_client:
+    dependency: "direct main"
+    description:
+      name: mqtt_client
+      sha256: "41c8edd3bc8efc80c1c8ebfb40081c24d12d13085faca96b9280a624eca2d893"
+      url: "https://pub.dev"
+    source: hosted
+    version: "10.11.11"
   nested:
     dependency: transitive
     description:
@@ -826,6 +906,14 @@ packages:
       url: "https://pub.dev"
     source: hosted
     version: "0.7.6"
+  timezone:
+    dependency: transitive
+    description:
+      name: timezone
+      sha256: dd14a3b83cfd7cb19e7888f1cbc20f258b8d71b54c06f79ac585f14093a287d1
+      url: "https://pub.dev"
+    source: hosted
+    version: "0.10.1"
   typed_data:
     dependency: transitive
     description:
diff --git a/smart_home_app/pubspec.yaml b/smart_home_app/pubspec.yaml
index dc424c2..be98633 100644
--- a/smart_home_app/pubspec.yaml
+++ b/smart_home_app/pubspec.yaml
@@ -12,7 +12,6 @@ dependencies:
   cupertino_icons: ^1.0.8
   provider: ^6.1.5+1
   intl: ^0.20.2
-  google_fonts: ^6.3.2
   flutter_iconly: ^1.0.2
   lucide_icons: ^0.257.0
   flutter_localizations:
@@ -24,6 +23,9 @@ dependencies:
   shared_preferences: ^2.5.5
   flutter_secure_storage: ^9.2.2
   local_auth: ^3.0.1
+  mqtt_client: ^10.11.11
+  flutter_local_notifications: ^20.1.0
+  flutter_background_service: ^5.1.0
 
 dev_dependencies:
   flutter_test:
@@ -51,9 +53,25 @@ flutter:
   assets:
     - assets/images/
   
-#   fonts:
-#     - family: DIN Next Arabic
-#       fonts:
-#         - asset: assets/fonts/DINNextLTArabic-Regular.ttf
-#         - asset: assets/fonts/DINNextLTArabic-Bold.ttf
-#           weight: 700
+  fonts:
+    - family: Montserrat
+      fonts:
+        - asset: assets/fonts/Montserrat-Regular.ttf
+        - asset: assets/fonts/Montserrat-Light.ttf
+          weight: 300
+    - family: Inter
+      fonts:
+        - asset: assets/fonts/Inter-Regular.ttf
+        - asset: assets/fonts/Inter-Medium.ttf
+          weight: 500
+        - asset: assets/fonts/Inter-SemiBold.ttf
+          weight: 600
+        - asset: assets/fonts/Inter-Bold.ttf
+          weight: 700
+    - family: Noto Sans Arabic
+      fonts:
+        - asset: assets/fonts/NotoSansArabic-Regular.ttf
+        - asset: assets/fonts/NotoSansArabic-Medium.ttf
+          weight: 500
+        - asset: assets/fonts/NotoSansArabic-Bold.ttf
+          weight: 700
